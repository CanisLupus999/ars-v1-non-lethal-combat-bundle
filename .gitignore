# Foundry VTT Compendium Database Files
# Ignore database lock and log files, but keep .ldb files (compendium data)
# Keep CURRENT and MANIFEST files in packs/ directories (needed for compendiums to work)
**/LOCK
**/LOG
**/LOG.old
# Allow CURRENT and MANIFEST files in packs directories (required for compendiums)
!packs/**/CURRENT
!packs/**/MANIFEST-*

# OS Files
.DS_Store
Thumbs.db
desktop.ini

# Editor Files
.vscode/
.idea/
*.swp
*.swo
*~

# Node modules (if you add any build tools later)
node_modules/
package-lock.json

